[pytest]
markers =
  unit: unit tests (no I/O)
  integration: integration tests (real Neo4j)
  e2e: end-to-end pipeline tests
  security: CVE/Cypher validation tests
  live: tests that require a live Neo4j instance
  asyncio: async tests (requires pytest-asyncio or equivalent)
